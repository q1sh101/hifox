// --- Spotify-specific overrides (base locks these, relaxed here for playback) ---
lockPref("dom.webaudio.enabled", true);              // audio processing/playback
lockPref("media.eme.enabled", true);                 // DRM (Widevine)
lockPref("media.gmp-widevinecdm.enabled", true);     // Widevine CDM plugin
lockPref("media.hardwaremediakeys.enabled", true);   // play/pause/next keyboard keys

